import Link from "next/link";

import { LedgerPanel, LedgerRow, LedgerRows } from "@/components/app/ledger";
import { Badge } from "@/components/ui/badge";
import type { F1PublicPool } from "@/lib/ranked-finish/persistence";

export function F1PublicPanels({ pool }: { pool: F1PublicPool }) { const leader = pool.latestStandings[0]; return <><LedgerPanel><LedgerRows className="grid md:grid-cols-4 md:divide-x md:divide-y-0"><LedgerRow><p className="text-xs font-medium uppercase text-muted-foreground">Entries</p><p className="mt-2 text-3xl font-semibold text-brand-ink">{pool.entries.length}</p></LedgerRow><LedgerRow><p className="text-xs font-medium uppercase text-muted-foreground">Leader</p><p className="mt-2 text-2xl font-semibold text-brand-ink">{leader?.entryName ?? "TBD"}</p><p className="text-sm text-muted-foreground">{leader ? `${leader.total} points` : "Awaiting results"}</p></LedgerRow><LedgerRow><p className="text-xs font-medium uppercase text-muted-foreground">Results entered</p><p className="mt-2 text-3xl font-semibold text-brand-ink">{Object.values(pool.settings.results).reduce((count, positions) => count + positions.length, 0)}/6</p></LedgerRow><LedgerRow><p className="text-xs font-medium uppercase text-muted-foreground">Event</p><p className="mt-2 text-xl font-semibold text-brand-ink">{pool.settings.basics.eventLabel}</p></LedgerRow></LedgerRows></LedgerPanel><section className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_22rem] xl:items-start"><LedgerPanel title="Result board" description="Qualifying and race positions are recorded in order."><LedgerRows>{pool.settings.markets.flatMap((market) => Array.from({ length: market.positions }, (_, index) => { const driver = pool.settings.competitors.find((candidate) => candidate.id === pool.settings.results[market.id]?.[index]); return <LedgerRow key={`${market.id}-${index + 1}`} className="flex items-center justify-between gap-4"><div><p className="font-semibold text-brand-ink">{market.label} P{index + 1}</p><p className="text-sm text-muted-foreground">{driver?.name ?? "Pending"}</p></div><Badge variant={driver ? "secondary" : "outline"}>{driver ? "Recorded" : "Pending"}</Badge></LedgerRow>; }))}</LedgerRows></LedgerPanel><F1Leaderboard pool={pool} /></section></>; }
export function F1Leaderboard({ pool }: { pool: F1PublicPool }) { return <LedgerPanel title="Leaderboard" description="Standings update whenever a position is recorded."><LedgerRows>{pool.latestStandings.length ? pool.latestStandings.map((row) => <LedgerRow key={row.entryId} className="flex items-center justify-between gap-4"><div><p className="font-semibold text-brand-ink">{row.rank}. {row.entryName}</p><p className="text-sm text-muted-foreground">Max {row.maxPoints} points</p></div><p className="text-xl font-bold text-brand-ink">{row.total}</p></LedgerRow>) : pool.entries.map((entry) => <LedgerRow key={entry.entryId}><p className="font-semibold text-brand-ink">{entry.entryName}</p><p className="text-sm text-muted-foreground">Submitted predictions awaiting results.</p></LedgerRow>)}{!pool.entries.length ? <LedgerRow><p className="text-sm text-muted-foreground">No submitted predictions yet.</p></LedgerRow> : null}</LedgerRows><div className="border-t px-5 py-4"><Link href={`/pools/${pool.poolSlug}/leaderboard`} className="text-sm font-semibold text-brand-ink hover:text-brand-hot">View full standings</Link></div></LedgerPanel>; }
