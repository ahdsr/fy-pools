import { RouteLoadingScreen } from "@/components/app/route-loading-screen";

export default function Loading() {
  return (
    <RouteLoadingScreen
      eyebrow="Account"
      title="Create your PoolWaffle account"
      description="Start a pool workspace, invite players, and publish your own live standings page."
    />
  );
}
