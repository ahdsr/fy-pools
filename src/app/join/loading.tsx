import { RouteLoadingScreen } from "@/components/app/route-loading-screen";

export default function Loading() {
  return (
    <RouteLoadingScreen
      eyebrow="Pool invite"
      title="Pool invite"
      description="Checking invite details and preparing the pick form."
    />
  );
}
